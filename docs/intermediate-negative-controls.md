# Intermediate build: negative controls

Generated by `tools/capture_intermediate_negative_controls.py`. Do not edit by hand;
a test compares this file against a fresh render.

Each case below takes the real build plan for the committed synthetic demo delivery,
replaces one substring in one named statement, and runs the real build. Nothing is
stubbed and no check is bypassed. The expected set of failing checks is declared per
case and compared exactly, so a case that starts failing for an extra reason -- or
stops failing at all -- breaks rather than quietly becoming decoration.

Three ways a case would be worthless, each reported rather than passed: the substring
was not in the SQL, so nothing was mutated; the mutated SQL did not compile, so no
check ran; or the build completed cleanly, which means the derivation was broken and
nothing noticed.

## Baseline: the unmutated build

The positive control. Without it, a document in which everything fails proves nothing about the mutations.

**Every check passes: yes**

```
engagement-kernel intermediate build

day boundary timezone : America/New_York
article view          : demo-article-view-v1
  content types       : article, liveblog
  event kinds         : content_delivery
unresolved section    : __unresolved__
time rate floor       : 3 deliveries

feature set: full
  + spine (from reader)
  + consumption (from reader_event)
  + topic (from content)
  + spine (from subscription_span)
  + email_cadence (from email_click)
  + deliverability (from email_open)
  + community (from community_action)

tables built:
  content_dimension (internal): 10 rows
  reader_content_day (internal): 17 rows
  reader_channel_day (published): 14 rows
  reader_section_day (published): 21 rows
  reader_email_day (published): 10 rows
  reader_community_day (published): 4 rows
  subscription_state_interval (published): 16 rows

checks:
  [PASS] dedup_keys_are_unique[content_dimension]: content_dimension keyed on (content_id) -- One row per piece of content.
  [PASS] dedup_keys_are_unique[reader_content_day]: reader_content_day keyed on (reader_id, channel, content_id, local_date) -- One row per reader per channel per piece of content per local day.
  [PASS] dedup_keys_are_unique[reader_channel_day]: reader_channel_day keyed on (reader_id, channel, local_date) -- One row per reader per channel per local day.
  [PASS] dedup_keys_are_unique[reader_section_day]: reader_section_day keyed on (reader_id, section, local_date) -- One row per reader per section per local day.
  [PASS] dedup_keys_are_unique[reader_email_day]: reader_email_day keyed on (list_id, reader_id, local_date) -- One row per list per reader per local day.
  [PASS] dedup_keys_are_unique[reader_community_day]: reader_community_day keyed on (reader_id, local_date) -- One row per reader per local day.
  [PASS] dedup_keys_are_unique[subscription_state_interval]: subscription_state_interval keyed on (reader_id, start_ts) -- One row per reader per state interval.
  [PASS] sessions_are_maximised_not_summed: reader_channel_day.sessions equals the distinct session count per reader, channel and local day, recomputed from the event layer
  [PASS] one_timezone_applied_to_reader_events: every reader-channel day in the channel table is a day the configured zone (America/New_York) produces from the event instants
  [PASS] section_attribution_reconciles: reader_section_day attributed views reconcile to the day's actual views for every reader, within 1e-09
  [PASS] unresolved_metadata_is_its_own_outcome: every view of content whose section metadata did not resolve lands on '__unresolved__', and nothing else does
  [PASS] section_is_never_null: reader_section_day.section is never null
  [PASS] one_timezone_applied_to_email: every reader-and-list day in the email table is a day the configured zone produces from the click instants -- the channel the upstream system left unconverted
  [PASS] one_timezone_applied_to_community: every reader day in the community table is a day the configured zone produces from the action instants -- the other unconverted channel
```

## Mutations

### Channel-day sessions summed instead of maximised

- **case** `sessions-summed-not-maximised`
- **statement** `reader_channel_day`
- **mutation**

```diff
- MAX(distinct_sessions_day) AS sessions
+ CAST(SUM(sessions) AS BIGINT) AS sessions
```

**What the wrong version does.** A reader who read three articles in one visit is recorded as having had three sessions. The number is larger, entirely plausible, and every views-per-session rate divides by it -- so the reader looks like a habitual short visitor instead of one deep reader, and no total anywhere is wrong.

- **expected failures** `sessions_are_maximised_not_summed`
- **actual failures** `sessions_are_maximised_not_summed`
- **as expected** yes

```
intermediate build failed 1 check(s): sessions_are_maximised_not_summed

[FAIL] sessions_are_maximised_not_summed: reader_channel_day.sessions equals the distinct session count per reader, channel and local day, recomputed from the event layer
  2 reader-channel-day row(s) carry a session count that is not the distinct session count. The usual cause is summing the per-content sessions column instead of maximising the carried per-day count
    {'reader_id': 'rdr-a1c7', 'channel': 'web', 'local_date': datetime.date(2026, 2, 16), 'emitted_sessions': 3, 'expected_sessions': 2}
    {'reader_id': 'rdr-b2d8', 'channel': 'web', 'local_date': datetime.date(2026, 2, 17), 'emitted_sessions': 2, 'expected_sessions': 1}
```

### Full weight to every section instead of 1/n

- **case** `section-attribution-full-weight`
- **statement** `reader_section_day`
- **mutation**

```diff
- 1.0 / len(w.effective_sections) AS section_weight
+ 1.0 AS section_weight
```

**What the wrong version does.** A view of an article filed under two sections becomes two views. Readers of heavily cross-filed content get inflated topic profiles, and the inflation correlates with the desk that files under most sections rather than with anything the reader did.

- **expected failures** `section_attribution_reconciles`
- **actual failures** `section_attribution_reconciles`
- **as expected** yes

```
intermediate build failed 1 check(s): section_attribution_reconciles

[FAIL] section_attribution_reconciles: reader_section_day attributed views reconcile to the day's actual views for every reader, within 1e-09
  5 reader-day(s) do not reconcile. A full-weight attribution multiplies a reader's day by the number of sections their content happened to be filed under; a dropped row loses the reading entirely. The FULL OUTER JOIN is what catches the second case: a reader-day missing from one side is a mismatch, not an absence
    {'reader_id': 'rdr-a1c7', 'local_date': datetime.date(2026, 2, 16), 'attributed_views': Decimal('5.0'), 'actual_views': 3, 'difference': Decimal('2.0')}
    {'reader_id': 'rdr-a1c7', 'local_date': datetime.date(2026, 3, 2), 'attributed_views': Decimal('2.0'), 'actual_views': 1, 'difference': Decimal('1.0')}
    {'reader_id': 'rdr-d4f0', 'local_date': datetime.date(2026, 3, 8), 'attributed_views': Decimal('2.0'), 'actual_views': 1, 'difference': Decimal('1.0')}
    {'reader_id': 'rdr-e5a1', 'local_date': datetime.date(2026, 2, 23), 'attributed_views': Decimal('2.0'), 'actual_views': 1, 'difference': Decimal('1.0')}
    {'reader_id': 'rdr-f6b2', 'local_date': datetime.date(2026, 2, 24), 'attributed_views': Decimal('2.0'), 'actual_views': 1, 'difference': Decimal('1.0')}
```

### Unresolved metadata dropped instead of routed to the sentinel

- **case** `unresolved-collapsed-to-zero`
- **statement** `reader_section_day`
- **mutation**

```diff
- ELSE ['__unresolved__']
+ ELSE []
```

**What the wrong version does.** A reader whose reading was all on content nobody could categorise is reported as having read nothing. The downstream reason codes cannot then tell a metadata gap from a quiet reader, and the two get the same treatment.

**On the expected failures.** Two checks, and both are correct. Dropping the rows loses the views, so the day no longer reconciles; and it loses the sentinel, so the unresolved volume has nowhere to be. The next case is the version that reconciles perfectly.

- **expected failures** `section_attribution_reconciles`, `unresolved_metadata_is_its_own_outcome`
- **actual failures** `section_attribution_reconciles`, `unresolved_metadata_is_its_own_outcome`
- **as expected** yes

```
intermediate build failed 2 check(s): section_attribution_reconciles, unresolved_metadata_is_its_own_outcome

[FAIL] section_attribution_reconciles: reader_section_day attributed views reconcile to the day's actual views for every reader, within 1e-09
  2 reader-day(s) do not reconcile. A full-weight attribution multiplies a reader's day by the number of sections their content happened to be filed under; a dropped row loses the reading entirely. The FULL OUTER JOIN is what catches the second case: a reader-day missing from one side is a mismatch, not an absence
    {'reader_id': 'rdr-b2d8', 'local_date': datetime.date(2026, 2, 17), 'attributed_views': 1.0, 'actual_views': 2, 'difference': -1.0}
    {'reader_id': None, 'local_date': None, 'attributed_views': None, 'actual_views': 1, 'difference': None}

[FAIL] unresolved_metadata_is_its_own_outcome: every view of content whose section metadata did not resolve lands on '__unresolved__', and nothing else does
  2 reader-day(s) disagree. A row present on the left only means real reading was attributed to the sentinel; a row present on the right only means unresolved reading was dropped or filed under a real section. The second is invisible to every total in this build
    {'reader_id': 'rdr-b2d8', 'local_date': datetime.date(2026, 2, 17), 'sentinel_views': None, 'unresolved_views': 1}
    {'reader_id': 'rdr-b2d8', 'local_date': datetime.date(2026, 2, 19), 'sentinel_views': None, 'unresolved_views': 1}
```

### Unresolved metadata filed under a real section

- **case** `unresolved-folded-into-a-real-section`
- **statement** `reader_section_day`
- **mutation**

```diff
- ELSE ['__unresolved__']
+ ELSE ['news']
```

**What the wrong version does.** Reading nobody could categorise is reported as interest in whichever section was chosen. Every total in the build still reconciles exactly, the section is a real one, and the topic profile is wrong in a direction somebody picked.

**On the expected failures.** This is the case the reconciliation check cannot see, which is why the sentinel has a check of its own. Views are conserved; only their destination is wrong.

- **expected failures** `unresolved_metadata_is_its_own_outcome`
- **actual failures** `unresolved_metadata_is_its_own_outcome`
- **as expected** yes

```
intermediate build failed 1 check(s): unresolved_metadata_is_its_own_outcome

[FAIL] unresolved_metadata_is_its_own_outcome: every view of content whose section metadata did not resolve lands on '__unresolved__', and nothing else does
  2 reader-day(s) disagree. A row present on the left only means real reading was attributed to the sentinel; a row present on the right only means unresolved reading was dropped or filed under a real section. The second is invisible to every total in this build
    {'reader_id': 'rdr-b2d8', 'local_date': datetime.date(2026, 2, 17), 'sentinel_views': None, 'unresolved_views': 1}
    {'reader_id': 'rdr-b2d8', 'local_date': datetime.date(2026, 2, 19), 'sentinel_views': None, 'unresolved_views': 1}
```

### Reader events bucketed by a bare cast instead of the configured zone

- **case** `day-boundary-from-the-session-zone`
- **statement** `ek_event_layer`
- **mutation**

```diff
- CAST(e.event_ts AT TIME ZONE 'America/New_York' AS DATE)
+ CAST(e.event_ts AS DATE)
```

**What the wrong version does.** Reading days come from whatever zone the session happens to be set to. On a developer's machine set to the publisher's own zone the answer is right, so the defect ships; in CI, or in another office, evening reads move to the next day and Sunday-evening reads move to the next week's bin.

**On the expected failures.** Only one check fails, and that is the point: every measure downstream is bucketed consistently by the wrong day, so the sessions, attribution and sentinel checks all agree with each other. Internal consistency is not correctness here.

- **expected failures** `one_timezone_applied_to_reader_events`
- **actual failures** `one_timezone_applied_to_reader_events`
- **as expected** yes

```
intermediate build failed 1 check(s): one_timezone_applied_to_reader_events

[FAIL] one_timezone_applied_to_reader_events: every reader-channel day in the channel table is a day the configured zone (America/New_York) produces from the event instants
  3 key-and-day combination(s) appear on one side only, so this channel's days were not bucketed in America/New_York. This is the defect that shifts one channel's whole history by hours while every number stays plausible
    {'reader_id': 'rdr-a1c7', 'channel': 'web', 'emitted_local_date': datetime.date(2026, 2, 17), 'expected_local_date': None}
    {'reader_id': 'rdr-d4f0', 'channel': 'web', 'emitted_local_date': datetime.date(2026, 3, 9), 'expected_local_date': None}
    {'reader_id': 'rdr-d4f0', 'channel': 'web', 'emitted_local_date': None, 'expected_local_date': datetime.date(2026, 3, 8)}
```

### Email days left in the vendor's own zone

- **case** `email-day-left-unconverted`
- **statement** `reader_email_day`
- **mutation**

```diff
- CAST(event_ts AT TIME ZONE 'America/New_York' AS DATE) AS local_date,
      1 AS click
+ CAST(event_ts AS DATE) AS local_date,
      1 AS click
```

**What the wrong version does.** This is the defect the upstream system actually has: web and app converted, email not converted at all. One channel's whole history sits hours away from the others, an evening click lands on the next day, and a Saturday-evening click lands in the following week's bin.

- **expected failures** `one_timezone_applied_to_email`
- **actual failures** `one_timezone_applied_to_email`
- **as expected** yes

```
intermediate build failed 1 check(s): one_timezone_applied_to_email

[FAIL] one_timezone_applied_to_email: every reader-and-list day in the email table is a day the configured zone produces from the click instants -- the channel the upstream system left unconverted
  2 key-and-day combination(s) appear on one side only, so this channel's days were not bucketed in America/New_York. This is the defect that shifts one channel's whole history by hours while every number stays plausible
    {'reader_id': 'rdr-d4f0', 'list_id': 'lst-daily', 'emitted_local_date': datetime.date(2026, 3, 9), 'expected_local_date': None}
    {'reader_id': 'rdr-d4f0', 'list_id': 'lst-daily', 'emitted_local_date': None, 'expected_local_date': datetime.date(2026, 3, 8)}
```

### Email clicks converted but opens left in the vendor's own zone

- **case** `email-open-day-left-unconverted`
- **statement** `reader_email_day`
- **mutation**

```diff
- CAST(event_ts AT TIME ZONE 'America/New_York' AS DATE) AS local_date,
      0 AS click
+ CAST(event_ts AS DATE) AS local_date,
      0 AS click
```

**What the wrong version does.** The half-converted version, and the more likely one: somebody fixes the channel they were looking at and leaves its neighbour. Reachability then reports a different set of days from engagement on the same feed.

**On the expected failures.** This case only discriminates because the demo delivery carries an open near local midnight on a day the same reader did not click. Without such a row the check would pass whether or not the conversion was applied, and the control would be reported as invalid rather than as evidence.

- **expected failures** `one_timezone_applied_to_email`
- **actual failures** `one_timezone_applied_to_email`
- **as expected** yes

```
intermediate build failed 1 check(s): one_timezone_applied_to_email

[FAIL] one_timezone_applied_to_email: every reader-and-list day in the email table is a day the configured zone produces from the click instants -- the channel the upstream system left unconverted
  2 key-and-day combination(s) appear on one side only, so this channel's days were not bucketed in America/New_York. This is the defect that shifts one channel's whole history by hours while every number stays plausible
    {'reader_id': 'rdr-a1c7', 'list_id': 'lst-daily', 'emitted_local_date': datetime.date(2026, 3, 9), 'expected_local_date': None}
    {'reader_id': 'rdr-a1c7', 'list_id': 'lst-daily', 'emitted_local_date': None, 'expected_local_date': datetime.date(2026, 3, 8)}
```

### Community days left in the vendor's own zone

- **case** `community-day-left-unconverted`
- **statement** `reader_community_day`
- **mutation**

```diff
- CAST(event_ts AT TIME ZONE 'America/New_York' AS DATE) AS local_date
+ CAST(event_ts AS DATE) AS local_date
```

**What the wrong version does.** The other half of the same upstream defect. Community is the smallest-variance feature block, so a shifted day does not weaken the signal so much as move it onto the wrong day of the week.

- **expected failures** `one_timezone_applied_to_community`
- **actual failures** `one_timezone_applied_to_community`
- **as expected** yes

```
intermediate build failed 1 check(s): one_timezone_applied_to_community

[FAIL] one_timezone_applied_to_community: every reader day in the community table is a day the configured zone produces from the action instants -- the other unconverted channel
  2 key-and-day combination(s) appear on one side only, so this channel's days were not bucketed in America/New_York. This is the defect that shifts one channel's whole history by hours while every number stays plausible
    {'reader_id': 'rdr-a1c7', 'emitted_local_date': datetime.date(2026, 3, 9), 'expected_local_date': None}
    {'reader_id': 'rdr-a1c7', 'emitted_local_date': None, 'expected_local_date': datetime.date(2026, 3, 8)}
```

### Section table grouped by content as well, breaking its grain

- **case** `section-grain-split-by-content`
- **statement** `reader_section_day`
- **mutation**

```diff
- GROUP BY reader_id, section, local_date
+ GROUP BY reader_id, section, local_date, content_id
```

**What the wrong version does.** Several rows per reader-section-day. Every window that sums this table is still correct, because the sum is unchanged -- but every one that counts rows, or joins to it, multiplies. A documented grain that nothing enforces is how this arrives.

**On the expected failures.** The reconciliation and sentinel checks both pass, because the views are conserved and merely spread over more rows. Only the grain is wrong, and only the grain check sees it.

- **expected failures** `dedup_keys_are_unique[reader_section_day]`
- **actual failures** `dedup_keys_are_unique[reader_section_day]`
- **as expected** yes

```
intermediate build failed 1 check(s): dedup_keys_are_unique[reader_section_day]

[FAIL] dedup_keys_are_unique[reader_section_day]: reader_section_day keyed on (reader_id, section, local_date) -- One row per reader per section per local day.
  1 key value(s) appear on more than one row. Every count built on this table is multiplied by the duplication, and no single number looks wrong
    {'reader_id': 'rdr-a1c7', 'section': 'news', 'local_date': datetime.date(2026, 2, 16), 'rows_per_key': 2}
```

## Refusals

Not mutations. These are builds the code declines to run because it was not told something it will not guess. They are captured here for the same reason the mutations are: "fails loudly when unset" is a claim, and a claim needs the message.

### No article-view definition at all

- **case** `article-view-unset`
- **expected** `BuildConfigError`
- **raised** `BuildConfigError`
- **as expected** yes

**Why it refuses.** A placeholder here produces a build that runs and is wrong, which is the failure this repository exists to prevent. There is no default and no fallback.

```
article_view is required and has no default. Which deliveries count as an article view is an editorial decision; a placeholder produces a build that runs and is wrong
```

### An article-view definition that counts no content type

- **case** `article-view-selects-nothing`
- **expected** `BuildConfigError`
- **raised** `BuildConfigError`
- **as expected** yes

**Why it refuses.** An empty selection is worse than an absent one: the build succeeds and reports every reader as inactive, which is indistinguishable from a quiet publisher.

```
article_view names no content types or no event kinds, so it selects nothing. An article-view definition that counts nothing reports every reader as inactive, which is indistinguishable from a quiet publisher
```

### A manifest whose article-view block counts no content type

- **case** `manifest-article-view-selects-nothing`
- **expected** `ManifestError`
- **raised** `ManifestError`
- **as expected** yes

**Why it refuses.** The contract refuses it at the point the definition is parsed, so an empty selection cannot reach the build at all. This is where the rule belongs: the build's own guard below is the backstop for a config built by hand.

```
article_view.content_types must name at least one content type
```

### A delivery whose manifest omits the article-view block

- **case** `manifest-without-article-view`
- **expected** `ManifestError`
- **raised** `ManifestError`
- **as expected** yes

**Why it refuses.** The definition has to travel with the data, so a published number can be traced to the definition it was produced under.

```
manifest.json is missing required key 'article_view'. It declares which content types and event kinds count as reading an article, plus a definition_id that travels with the answer. It changes every view-based feature. Owned by the newsroom, not by engineering. There is no default: start from examples/manifest-template.json, which ships every required key with its answer left open, and see docs/declarations-questionnaire.md for the question and who owns it
```

### A delivery whose manifest declares no day-boundary timezone

- **case** `manifest-without-timezone`
- **expected** `ManifestError`
- **raised** `ManifestError`
- **as expected** yes

**Why it refuses.** The two plausible guesses differ by hours. Guessing mis-buckets every window without anything visibly breaking.

```
day_boundary_timezone must be a non-empty IANA timezone name. There is no default: which timezone defines a day is a publisher decision, and guessing it mis-buckets every window without anything visibly breaking
```

### A delivery missing a required contract table

- **case** `missing-required-input`
- **expected** `MissingRequiredInput`
- **raised** `MissingRequiredInput`
- **as expected** yes

**Why it refuses.** Nothing is built. An empty stand-in would let the build finish and report a publisher with no content.

```
the delivery is missing required contract table(s): content. Nothing was built: an empty stand-in would report a publisher with no readers
```

### A control aimed at a statement this build does not run

- **case** `override-of-a-statement-that-never-runs`
- **expected** `KeyError`
- **raised** `KeyError`
- **as expected** yes

**Why it refuses.** This one guards the harness rather than the build. A mutation aimed at a statement that never executes would pass for free and read as evidence.

```
"statement_overrides names statements this build does not run: ['user_device_day']. A control aimed at a statement that never executes passes for free"
```
