"""Canonical columnar input contract: schemas, validation, and loading.

Empty for now. The contract itself is defined in a separate change; this package
exists so the layout, packaging and CI gate are in place first.
"""
